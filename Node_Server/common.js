// Terminator Code
const TERMINATOR = 10;

/**
 * return ASCII code from char
 * @param {char} a 
 * @returns {int}
 */
function get_ascii(a) {
    return a.charCodeAt(0);
}

module.exports = {
    TERMINATOR: TERMINATOR,
    get_ascii: get_ascii
}






